---
phase: 02-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils.ts
  - src/components/layout/Navigation.tsx
  - src/components/sections/Hero.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "Navigation displays logo and section links (Services, Projects, Contact)"
    - "Navigation starts transparent over hero"
    - "Navigation becomes solid with shadow when scrolled past 50px"
    - "Hero displays empathy-first headline acknowledging visitor wariness"
    - "Hero displays value proposition below headline"
    - "Hero displays subtle scroll hint with arrow"
    - "Hero conveys atmospheric 'workshop at golden hour' mood"
  artifacts:
    - path: "src/lib/utils.ts"
      provides: "cn() utility for conditional class merging"
      exports: ["cn"]
    - path: "src/components/layout/Navigation.tsx"
      provides: "Scroll-aware navigation bar"
      exports: ["Navigation"]
    - path: "src/components/sections/Hero.tsx"
      provides: "Hero section with empathy-first messaging"
      exports: ["Hero"]
  key_links:
    - from: "src/components/layout/Navigation.tsx"
      to: "src/lib/utils.ts"
      via: "import { cn }"
      pattern: "import.*cn.*from.*lib/utils"
    - from: "src/components/sections/Hero.tsx"
      to: "anchor href"
      via: "scroll hint link"
      pattern: "href=['\"]#services['\"]"
---

<objective>
Create the foundational components for Phase 2: utility function, scroll-aware navigation, and empathy-first hero section.

Purpose: Establish the primary content structure that visitors see first — navigation for wayfinding and hero for emotional connection. The hero must make weary visitors feel understood before impressed.
Output: Three components ready to be composed into the main page, plus lucide-react installed for service icons.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content/02-CONTEXT.md
@.planning/phases/02-content/02-RESEARCH.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create utility function and install lucide-react</name>
  <files>src/lib/utils.ts, package.json</files>
  <action>
    1. Install lucide-react: `npm install lucide-react`
    2. Create `src/lib/utils.ts` with cn() utility:
       - Import clsx and tailwind-merge (both already in package.json)
       - Export cn() function: `cn(...inputs) => twMerge(clsx(inputs))`
       - This is the standard shadcn/ui pattern for conditional class merging

    The cn() utility is used throughout components for conditional Tailwind classes.
  </action>
  <verify>
    - `npm ls lucide-react` shows lucide-react installed
    - `src/lib/utils.ts` exists with cn export
  </verify>
  <done>lucide-react installed, cn() utility available for import in all components</done>
</task>

<task type="auto">
  <name>Task 2: Create Navigation component</name>
  <files>src/components/layout/Navigation.tsx</files>
  <action>
    Create a client component with scroll-aware behavior:

    1. Component structure:
       - "use client" directive (requires useState/useEffect for scroll detection)
       - Fixed position header with z-50
       - Logo on left ("Civix" in serif font, links to "/")
       - Nav links on right: Services, Projects, Contact (anchor links to #services, #projects, #contact)

    2. Scroll behavior:
       - useState for scrolled state (boolean)
       - useEffect with scroll listener, cleanup on unmount
       - Threshold: 50px
       - Initial check on mount (handleScroll() call)

    3. Visual states:
       - Not scrolled: bg-transparent, py-4
       - Scrolled: bg-base-900/95 backdrop-blur-sm shadow-elevation-sm py-3
       - Transition: transition-all duration-300

    4. Styling:
       - Max width 6xl, centered with px-[var(--container-padding)]
       - Logo: font-serif text-h3, hover:text-amber-400 transition
       - Links: text-text-secondary hover:text-text-primary transition, gap-8

    Use cn() from lib/utils for conditional class merging.
  </action>
  <verify>
    - `src/components/layout/Navigation.tsx` exists
    - Component has "use client" directive
    - Component imports cn from lib/utils
    - Component uses useState and useEffect
  </verify>
  <done>Navigation component renders fixed header that transitions from transparent to solid on scroll</done>
</task>

<task type="auto">
  <name>Task 3: Create Hero section</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
    Create a server component (no "use client" needed — purely presentational):

    1. Section structure:
       - section element with id="hero" (for potential anchor links)
       - min-h-[85vh] to hint at content below
       - flex items-center for vertical centering
       - pt-20 to account for fixed nav height

    2. Atmospheric background layers (absolute positioned):
       - Base: bg-gradient-to-b from-base-900 via-base-950 to-base-950
       - Warm glow: positioned top-1/4 -left-1/4, w-3/4 h-1/2, rounded-full blur-[120px] opacity-30
         Use radial-gradient with oklch(0.75 0.12 72 / 0.4) for golden hour warmth

    3. Content (relative z-10):
       - Max width container (6xl) with container padding
       - Inner max-w-3xl for text width

    4. Copy (empathy-first, per CONTEXT.md):
       - Headline (text-display font-serif): "Been burned by developers before?"
         This acknowledges their pain immediately (HERO-01)
       - Value proposition (text-h3 font-sans text-text-secondary, max-w-2xl):
         "I get it. You hired someone who promised the world and disappeared. Let's skip the sales pitch and talk about what you actually need."
         This validates and reassures (HERO-02)

    5. Scroll hint:
       - Anchor link to #services
       - Text: "See how I can help" with down arrow character
       - Style: text-amber-400 hover:text-amber-300 inline-flex items-center gap-2
       - Arrow: group-hover:translate-y-0.5 transition-transform for subtle movement
  </action>
  <verify>
    - `src/components/sections/Hero.tsx` exists
    - Component does NOT have "use client" (server component)
    - Contains empathy-first headline "Been burned"
    - Contains scroll hint linking to #services
    - Contains atmospheric gradient background
  </verify>
  <done>Hero section displays empathy-first messaging with atmospheric golden hour mood and scroll hint</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All three files exist at correct paths
2. lucide-react appears in package.json dependencies
3. No TypeScript errors: `npx tsc --noEmit`
4. Components can be imported without error
</verification>

<success_criteria>
- cn() utility available and correctly merges Tailwind classes
- Navigation component is a client component with scroll detection
- Hero component is a server component with empathy-first copy
- All components use design system tokens from Phase 1 (text-display, text-h3, base-900, amber-400, etc.)
- Atmospheric background creates "workshop at golden hour" feel
- Scroll hint provides clear affordance to continue reading
</success_criteria>

<output>
After completion, create `.planning/phases/02-content/02-01-SUMMARY.md`
</output>
