---
phase: 03-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/project.ts
  - src/data/projects.ts
  - src/components/ui/dialog.tsx
autonomous: true

must_haves:
  truths:
    - "Project type definitions exist with live vs coming-soon discrimination"
    - "Sample project data compiles without TypeScript errors"
    - "shadcn/ui Dialog component is installed and available"
  artifacts:
    - path: "src/types/project.ts"
      provides: "Project type definitions with discriminated union"
      exports: ["Project", "LiveProject", "ComingSoonProject", "isLiveProject"]
    - path: "src/data/projects.ts"
      provides: "Sample project data array"
      exports: ["projects"]
    - path: "src/components/ui/dialog.tsx"
      provides: "shadcn/ui Dialog component"
      contains: "DialogContent"
  key_links:
    - from: "src/data/projects.ts"
      to: "src/types/project.ts"
      via: "import type { Project }"
      pattern: "satisfies.*Project"
---

<objective>
Install shadcn/ui Dialog component and create TypeScript data layer for projects.

Purpose: Establish type-safe foundation for project data that enables adding new projects without component modification.
Output: Dialog component installed, project types defined, sample data ready for UI consumption.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-projects/03-CONTEXT.md
@.planning/phases/03-projects/03-RESEARCH.md

Reference existing patterns:
@src/components/sections/Services.tsx (card pattern, data structure)
@src/lib/utils.ts (cn utility)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui Dialog and create project types</name>
  <files>src/components/ui/dialog.tsx, src/types/project.ts</files>
  <action>
1. Run `npx shadcn@latest add dialog` to install the Dialog component
   - This will create src/components/ui/dialog.tsx
   - May also create src/lib/utils.ts if not exists (already exists)

2. Create src/types/project.ts with discriminated union type:
   - BaseProject interface with shared fields: id, title, tagline, thumbnail, images[], challenge, approach, result, technologies[]
   - LiveProject extends BaseProject with status: 'live' and required url field
   - ComingSoonProject extends BaseProject with status: 'coming-soon' and optional githubUrl
   - Export union type Project = LiveProject | ComingSoonProject
   - Export type guard function isLiveProject()

Key field requirements from CONTEXT.md:
- thumbnail: card image path
- images: array for modal gallery
- challenge/approach/result: narrative structure
- technologies: array of tech names
- url: required for live projects only
- githubUrl: optional for coming-soon projects
  </action>
  <verify>
- src/components/ui/dialog.tsx exists after shadcn install
- `npx tsc --noEmit` passes with no type errors
- Project type exports are accessible from @/types/project
  </verify>
  <done>Dialog component installed, project types defined with discriminated union pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create sample project data file</name>
  <files>src/data/projects.ts</files>
  <action>
Create src/data/projects.ts with sample project data:

1. Import Project type from @/types/project
2. Create projects array with 4 sample projects:
   - 2 live projects (with quantified results per PROJ-03)
   - 2 coming-soon projects (with GitHub links)

Sample project structure from RESEARCH.md:
```typescript
export const projects = [
  {
    id: 'employee-management',
    title: 'Employee Management Dashboard',
    tagline: 'Transformed HR chaos into streamlined operations',
    thumbnail: '/projects/employee-management/thumbnail.jpg',
    images: [
      '/projects/employee-management/screenshot-1.jpg',
      '/projects/employee-management/screenshot-2.jpg',
    ],
    status: 'live',
    url: 'https://example.com',
    challenge: 'A growing business was tracking employee hours, PTO, and schedules across three different spreadsheets...',
    approach: 'Built a unified dashboard with real-time sync, automated PTO calculations, and role-based access...',
    result: 'Reduced weekly HR admin time by 6 hours and eliminated double-booking errors completely.',
    technologies: ['Next.js', 'TypeScript', 'Supabase', 'Tailwind CSS'],
  },
  // ... more projects
] as const satisfies readonly Project[]
```

3. Use `as const satisfies readonly Project[]` pattern for compile-time validation

Important: Use placeholder image paths - actual images will be added later
Include at least one quantified outcome (e.g., "reduced time by X hours", "saved X per month")
  </action>
  <verify>
- `npx tsc --noEmit` passes with no type errors
- All 4 projects have required fields based on their status
- At least one live project has a specific quantified result (number, not vague)
  </verify>
  <done>Project data file created with 4 sample projects, type-safe and ready for UI consumption</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` - TypeScript compilation passes
2. Dialog component exists at src/components/ui/dialog.tsx
3. Project types export correctly: `import type { Project } from '@/types/project'`
4. Project data compiles: `import { projects } from '@/data/projects'`
</verification>

<success_criteria>
- shadcn/ui Dialog installed and configured for dark theme
- Project types with live/coming-soon discrimination
- 4 sample projects with placeholder data (2 live, 2 coming-soon)
- At least one project has quantified outcome (PROJ-03)
- TypeScript enforces structure at compile time (PROJ-05)
</success_criteria>

<output>
After completion, create `.planning/phases/03-projects/03-01-SUMMARY.md`
</output>
